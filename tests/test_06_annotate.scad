include <openscad_annotations/annotate.scad>

/*
module test_anno_model_vector_combo() {
    assert( anno_model_vector_combo( TOP ) == TOP+RIGHT+BACK ); 
    assert( anno_model_vector_combo( RIGHT ) == TOP+RIGHT+BACK ); 
    assert( anno_model_vector_combo( BACK ) == TOP+RIGHT+BACK ); 

    assert( anno_model_vector_combo( BOTTOM ) == BOTTOM+LEFT+FWD ); 
    assert( anno_model_vector_combo( LEFT ) == BOTTOM+LEFT+FWD ); 
    assert( anno_model_vector_combo( FWD ) == BOTTOM+LEFT+FWD ); 

    assert( anno_model_vector_combo( FWD+LEFT ) == BOTTOM+LEFT+FWD ); 

    assert( anno_model_vector_combo( FWD+RIGHT ) == UP );
}
test_anno_model_vector_combo();

*/

